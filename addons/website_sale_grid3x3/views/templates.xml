<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <template id="products_item_care" inherit_id="website_sale.products_item" name="Care Product Items">
            <xpath expr="//form[@action='/shop/cart/update']" position="replace">
                <form action="/shop/cart/update" method="post" class="product_item">
                    <div itemscope="itemscope" itemtype="http://schema.org/Product">
                        <div class="ribbon-wrapper">
                            <div class="ribbon btn btn-danger">Sale</div>
                        </div>
                        <div class="oe_product_image">
                            <a itemprop="url" t-att-href="keep('/shop/product/%s' % slug(product), page=(pager['page']['num'] if pager['page']['num']>1 else None))">
                                <img itemprop="image" class="img img-responsive" t-att-src="website.image_url(product, 'image', None if product_image_big else '300x300')" t-att-alt="product.name"/>
                            </a>
                        </div>
                        <section>
                            <h5>
                                <!-- Name -->
                                <strong><a itemprop="name" t-att-href="keep('/shop/product/%s' % slug(product), page=(pager['page']['num'] if pager['page']['num']>1 else None))" t-field="product.name"/></strong>
                                <!-- Price (float right) -->
                                <div itemprop="offers" itemscope="itemscope" itemtype="http://schema.org/Offer" class="product_price" t-if="product.product_variant_ids">
                                    <b>
                                        <!-- List Price -->
                                        <t t-if="(compute_currency(product.lst_price) - product.price) &gt; 0.1">
                                            <del class="text-danger" style="white-space: nowrap;" t-field="product.lst_price" t-field-options='{
                                              "widget": "monetary",
                                              "from_currency": "product.company_id.currency_id",
                                              "display_currency": "user_id.partner_id.property_product_pricelist.currency_id"
                                            }'/>&amp;nbsp;
                                        </t>
                                        <!-- Price and Currency -->
                                        <span t-field="product.price" style="white-space: nowrap;"  t-field-options='{
                                            "widget": "monetary",
                                            "display_currency": "user_id.partner_id.property_product_pricelist.currency_id"
                                            }'>
                                        </span>
                                        <!-- Hidden: only for form data -->
                                        <span itemprop="price" style="display:none;" t-esc="product.price"/>
                                        <span itemprop="priceCurrency" style="display:none;" t-esc="user_id.partner_id.property_product_pricelist.currency_id.name"/>
                                    </b>
                                </div>
                            </h5>
                            <!-- Product Description -->
                            <div class="text-info oe_subdescription" contenteditable="false">
                                <div itemprop="description" t-field="product.description_sale"></div>
                            </div>
                            <!-- Quick Add To Cart -->
                            <div class="quick_add_to_cart">
                                <input name="product_id" t-att-value="product.product_variant_ids[0].id" type="hidden"/>
                                <t t-if="len(product.product_variant_ids) == 1">
                                    <a class="btn btn-default btn-xs fa fa-shopping-cart a-submit"/>
                                </t>
                                <t t-if="len(product.product_variant_ids) &gt; 1">
                                    <a class="btn btn-default btn-xs fa fa-shopping-cart" t-att-href="keep('/shop/product/%s' % slug(product), page=(pager['page']['num'] if pager['page']['num']>1 else None))"/>
                                </t>
                            </div>
                        </section>
                    </div>
                </form>
            </xpath>
        </template>


        <!-- Load custom CSS and JS for the website -->
        <template id="assets_frontend_care_test" inherit_id="website.assets_frontend" name="TEST care_config Frontend CSS and JS">
            <!-- //form[@action='/shop/cart/update'] -->
            <xpath expr="." position="inside">
                <!-- Java Scripts -->
                <!--<script type="text/javascript" src="/customer_care/static/src/js/customer_care.js"></script>-->
                <!-- Style Sheets -->
                <link rel="stylesheet" href='/website_sale_grid3x3/static/src/css/test.css'/>
            </xpath>
        </template>

    </data>
</openerp>
